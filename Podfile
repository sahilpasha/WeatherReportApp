# Uncomment the next line to define a global platform for your project
  platform :ios, '10.2'

target 'WeatherReportApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 4.0'

  # Pods for WeatherReportApp

  target 'WeatherReportAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'WeatherReportAppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
